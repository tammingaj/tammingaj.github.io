import { SoundscapeDirective } from './soundscape.directive';

describe('SoundscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new SoundscapeDirective();
    expect(directive).toBeTruthy();
  });
});
